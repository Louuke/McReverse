package icu.jnet.mcd.constants;

public enum Action {

    ACCOUNT_DELETED,
    AUTHORIZATION_CHANGED,
    JWT_EXPIRED,
    JWT_INVALID,
    REQUEST_TIMED_OUT,
    TOKEN_REQUIRED,
    BASIC_BEARER_REQUIRED;
}
